# jenkinsfile

Set up a 'template' Jenkinsfile that serves various repos. The file integrates Groovy shared pipeline libraries.
The Jenkinsfile is set up as a submodule to the 'groovy-gradle-project'.
